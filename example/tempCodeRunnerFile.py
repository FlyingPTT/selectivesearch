    # # save
    # img_root = r'D:\Github\selectivesearch\out'
    # img_name = 'scale={}--sigma={}--min_size={}.png'.format(scale, sigma, min_size)
    # plt.savefig(os.path.join(img_root, img_name))